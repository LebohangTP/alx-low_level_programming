#include <stdio.h>

/**
 * main - main function
 * Return: always 0
 */

int main(void)
{
	int d, p;

	for (d = '0' ; d < '9' ; d++)
	
	{
	}

	for (p = d + 1; p <= '9' ; p++)
	putchar('\n');
	return (0);
}
